import React from 'react';
import CameraControlStyles from './CameraControls.scss';

const CameraControls = (props) => {
  return (
    <div className="camera-controls">
      <button className="shutter-button" />
    </div>
  );
};

export default CameraControls;
